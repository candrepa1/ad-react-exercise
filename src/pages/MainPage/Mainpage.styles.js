import { makeStyles } from "@material-ui/core";

export const useStylesMain = makeStyles({
	main: {
		margin: "0px 10px",
	},
	title: {
		fontWeight: "normal",
		fontFamily: "Helvetica",
		fontSize: 30,
	},
});
